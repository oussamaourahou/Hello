# API Keys (Optional - only needed for Menu AI features)
OPENAI_API_KEY=your_openai_api_key_here
PHOTOROOM_API_KEY=your_photoroom_api_key_here

# Database Configuration
DB_PATH=email_archive.db
DATA_DIR=./data

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Configuration (comma-separated list of allowed origins, use * for all)
CORS_ORIGINS=*
